FROM nginx:alpine

# Copy the pre-built Angular assets compiled on the host
COPY dist/frontend/browser /usr/share/nginx/html

# Copy the Nginx proxy and fallback configuration
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Expose port
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
