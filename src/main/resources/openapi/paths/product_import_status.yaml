get:
  tags:
    - Products
  summary: Check status and validation reports of a CSV import task (Admin only)
  operationId: getImportStatus
  parameters:
    - name: taskId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: Task import status report retrieved
      content:
        application/json:
          schema:
            $ref: '../components/schemas/products.yaml#/ProductImportStatusDto'
    '404':
      description: Import task not found
    '403':
      description: Forbidden (requires admin role)
